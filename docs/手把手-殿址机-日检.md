# 殿址机 · 每日开机检查（5 分钟）

> 昨晚已装三件套；今天开机跑一遍，回姜子牙 **「殿址机日检过了」**。

## 在 Git Bash 里逐项打勾

```bash
# 1. 三件套版本
cursor --version 2>/dev/null || echo "Cursor：从开始菜单确认能打开即可"
git --version
python --version

# 2. 仓库在不在（路径按你实际改）
cd ~/Jiangziyamemory 2>/dev/null || cd /c/Users/你的用户名/Jiangziyamemory
git status

# 3. 柏鉴
python scripts/bailian_digest.py

# 4. 语音栈（与 Mac 同一套）
bash scripts/install_voice_stack.sh
python scripts/voice_listen.py --once
# 说一句话测试，Ctrl+C 退出

# 5. 自动 push 钩子（若昨晚没装）
bash scripts/install-auto-push-hook.sh
```

## 通过标准

- [ ] Cursor 能开、能登录（与 Mac 同账号）  
- [ ] `git clone` 已完成，能 `git pull`  
- [ ] 柏鉴能生成 `tasks/晨间裁决-日期.md`  
- [ ] 语音常听能写入 `inbox/弘尊语音收件箱.md`  
- [ ] 可开 **New Agent** 读 `docs/agents/闻仲.md`

## 备注

- **游戏**：弘尊已令殿址机专项目用，无需再留 GPU 给游戏。  
- 故障把终端报错原样发给姜子牙。
