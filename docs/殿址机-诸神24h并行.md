# 殿址机 · 诸神 24h 并行

> 弘尊：台式机可常开；诸神不应等「今夜」；你是**红绿灯**，不是减速伞。  
> **v1（裁定 B）**：诸神位对弘尊界面**冻结**默认专窗/手贴派单；**本条姜子牙 = 真贾维斯**，仓库资料保留。见 [`贾维斯-v1-弘尊独享.md`](贾维斯-v1-弘尊独享.md)。  
> **忙闲 / 督办简表**：以 [`tasks/诸神并行工单.md`](../tasks/诸神并行工单.md) 为准；弘尊只听榜，不默认开诸神窗。  
> **殿址机**：cron / 脚本（如 `jike_watch`）可由贾维斯本窗或殿址机跑；**不**要求弘尊「睡前点 3 次 New Agent」为日常。  
> 下文「多开 Agent」作**可选后台**档案，与 v1 冲突时以 v1 为准。

---

## 三层自动化（由易到难）

### 1 · 柏鉴（写死程序，现在就有）

```bash
cd ~/Jiangziya/jiangziyamemory
python3 scripts/bailian_digest.py
```

→ 生成 `tasks/晨间裁决-YYYY-MM-DD.md`（记忆摘要 + 待办 + deliverables 列表）

**台式机 cron（每天 6:00）：**

```bash
crontab -e
# 加一行（路径按你台式机改）：
0 6 * * * cd /path/to/Jiangziyamemory && python3 scripts/bailian_digest.py
```

柏鉴的贡献：**你打开文件就能批**，不用翻整库。

---

### 2 · 诸神 Cursor Agent（殿址机常开）

- 台式机登录 Cursor，**多开 Agent 窗口**（比干 / 吴道子 / 闻仲 / …）  
- 各 @ 自己的 `docs/agents/<神>.md` + `tasks/诸神并行工单.md` + 当日 `tasks/诸神-立即落实-YYYY-MM-DD.md` 对应派单块  
- 你不在时：窗口可挂着，**但 Cursor 不会自动替你点 New Agent** —— 需：

  **A)** 你离开前说「启动并行」，姜子牙写好工单，你点 3 次 New Agent 各跑一轮  
  **B)** **Cursor Cloud / Background Agents**（若账号有）：云端续跑，早上看结果  
  **C)** 日后 **Cursor CLI + cron** 调 Agent（见 Cursor 文档，殿址机可试）

---

### 3 · 闻仲 push（已装 hook）

commit → 自动 push → Pages 更新。你只批「准」，不拖在队尾。

---

## 分工再强调

| 谁 | 角色 |
|----|------|
| **弘尊** | 红绿灯：准 / 续议 / 否 |
| **姜子牙** | 派单、汇总、推你批；弘尊开口即一轮扫并行表+立即落实表；**H1 未落盘 / T008 §三空则每轮催闻仲**；不包办忙闲（交金灵台账） |
| **金灵圣母** | 督办台账：忙闲、闲几天、卡点 |
| **魔礼青** | 待补证清单：T008/T009/H1/盯数 |
| **诸神 Agent** | 并行产出 deliverables |
| **柏鉴程序** | 定时摘要，不聊天也干活 |
| **闻仲** | 批「准」后改代码 |

---

## 「≤4 神在线」

已废止。台式机内存够就多开；乱了就靠工单号，不靠限人数。

---

*硬件常开是优势，要用起来。*
