# 吕岳工单 · 安全清单首单

## 交付

`deliverables/吕岳/安全清单-首单.md`

## 内容须含

1. **密钥类**：列出本仓库用到的环境变量名（如 `PUSHPLUS_TOKEN`），**不写值**；写「应只在 `.env` / 本机」。  
2. **禁止入库**：`.env`、`*.pem`、`credentials*`。  
3. **外发前脱敏**：发帖、截图、录屏各 3 条检查项（白话）。

## 截止

与金灵圣母首单同一批交。
